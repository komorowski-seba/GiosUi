import { Card, CardContent, Typography } from "@mui/material";

export default function Dashboard(): JSX.Element {
    return (
        <Card>
            <CardContent>
                <Typography variant="h4">Dashboard</Typography>
            <Typography>To jest pierwszy widok.</Typography>
            </CardContent>
        </Card>
    );
}